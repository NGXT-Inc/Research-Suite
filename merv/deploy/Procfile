# Process declaration for the hosted Merv control plane.
# The user-machine side is the stateless MCP proxy launched by the agent client,
# not a Procfile-managed daemon.

# Cloud control plane. Keep private behind a TLS-terminating load balancer.
# Needs MERV_DB_URL + MERV_BLOB_BUCKET.
control: merv-control
